import fs from 'node:fs/promises';
import path from 'node:path';
import simpleGit from 'simple-git';
import { CloneJob } from '../types/Repository.type';


export class CloneService {
    static async cloneRepo(job: CloneJob) {
        try {

        } catch (error) {

        }
    }
}